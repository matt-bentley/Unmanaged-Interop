#include "Math.h"
#include <string>
#include <iostream>
#include <numeric>
#include <algorithm>
#include <vector>
#include <fstream>
#include <ctime>
#include <iterator>
#include <sstream>

int math_add(int a, int b)
{
	int s1len = 3;
	int s2len = 3;

	/*auto column_start = (decltype(s1len))1;
	auto column = new decltype(s1len)[s1len + 1];*/

	return 0;
}
